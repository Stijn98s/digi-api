import { MongooseValidationFilter } from './mongoose-validation.filter';

describe('MongooseValidationFilterFilter', () => {
  it('should be defined', () => {
    expect(new MongooseValidationFilter()).toBeDefined();
  });
});
